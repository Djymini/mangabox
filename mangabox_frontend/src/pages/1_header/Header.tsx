import {FC} from 'react';
import Navbar from "../../components/header/navbar/Navbar";

const Header: FC<{}> = ({}) => {
    return (
        <header>
            <Navbar/>
        </header>
    );
};

export default Header;

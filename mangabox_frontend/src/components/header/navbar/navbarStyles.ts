import {paletteColor} from "../../../_themes/paletteColor";

export const navbarButtonStyle = {
    background: paletteColor.claret,
    color: 'white',
    '&:hover': {
        backgroundColor: paletteColor.blush,
    }
}

export const navbarIconStyle = {
    fontSize: '40px'
}